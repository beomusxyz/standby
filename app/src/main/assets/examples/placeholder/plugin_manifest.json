{
  "id": "string (unique identifier, e.g., 'com.example.myclock')",
  "name": "string (human-readable name)",
  "description": "string (brief explanation of functionality)",
  "author": "string (name of the creator)",
  "version": "string (semantic version, e.g., '1.0.0')",
  "size:": "half/full (size of the plugin, whether it handles 2 at the same time or just one)",
  "permissions": [
    "string (requested capability, e.g., 'battery', 'sensors', 'health')"
  ],
  "network_whitelist": [
    "string (allowed domain, e.g., 'api.example.com')"
  ],
  "min_app_version": "integer (reserved compatibility metadata; currently use 1)"
}
